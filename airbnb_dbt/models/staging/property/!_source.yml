version: 2

sources:
  - name: property
    database: airbnb_raw
    schema: property
    tables:
      - name: listings
      - name: neighbourhoods
        config:
          freshness:
            warn_after: {count: 12, period: hour}
          loaded_at_field: scraped_at
